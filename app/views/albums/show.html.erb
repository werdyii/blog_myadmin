<h2><%= @album.name %></h2>

<div class="photos">
	<% if @photos.any? %>
	<ul>
		<%= render @photos %>
	</ul>
	<div class="clear"></div>
	<% else %>
	  <p>V tomto albume niesu ešte fotky.</p>
	<% end %>
</div>
